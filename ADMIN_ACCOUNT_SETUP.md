# 👨‍💼 تعليمات إنشاء حساب الأدمن

## 📍 **الوضع الحالي:**
- ✅ صفحة الأدمن: **`/admin/login`**
- ❌ **لا يوجد حساب أدمن حالياً**
- 🔒 محتاج تنشئ حسابك الخاص بباسورد قوي

---

## 🔧 **طريقة إنشاء حساب الأدمن:**

### الخطوة 1️⃣: افتح Supabase Dashboard
1. اذهب إلى: [https://app.supabase.com](https://app.supabase.com)
2. اختر مشروعك
3. من القائمة الجانبية → **SQL Editor**
4. اضغط **+ New Query**

---

### الخطوة 2️⃣: نفّذ هذا الكود SQL

```sql
-- إنشاء جدول admin_users (إذا لم يكن موجوداً)
CREATE TABLE IF NOT EXISTS admin_users (
  id SERIAL PRIMARY KEY,
  email VARCHAR(255) UNIQUE NOT NULL,
  password_hash TEXT NOT NULL,
  name VARCHAR(255),
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  last_login TIMESTAMP WITH TIME ZONE
);
```

---

### الخطوة 3️⃣: توليد Hash للباسورد

**استخدم هذا الكود في Node.js** لتوليد hash آمن لباسوردك:

```javascript
const bcrypt = require('bcryptjs');
const password = 'اكتب_باسوردك_القوي_هنا';  // غيّر هذا!
const hash = bcrypt.hashSync(password, 10);
console.log('Password Hash:', hash);
```

**أو استخدم موقع أونلاين:**
- اذهب لـ: [https://bcrypt-generator.com/](https://bcrypt-generator.com/)
- اكتب باسوردك القوي
- اختر **Rounds: 10**
- احصل على الـ Hash

---

### الخطوة 4️⃣: أدخل بياناتك في الجدول

بعد ما تحصل على الـ Hash، نفّذ هذا SQL:

```sql
-- إدخال حساب الأدمن الخاص بك
INSERT INTO admin_users (email, password_hash, name)
VALUES (
  'your-email@example.com',           -- غيّر الإيميل هنا
  '$2b$10$YourHashWillBeHere...',     -- حط الـ Hash اللي طلّعته
  'Your Name'                         -- اسمك
);
```

---

### الخطوة 5️⃣: تسجيل الدخول

1. افتح الموقع
2. اذهب لـ **`/admin/login`**
3. استخدم:
   - **Email**: الإيميل اللي حطّيته
   - **Password**: الباسورد اللي استخدمته (مش الـ Hash!)

---

## ⚡ **طريقة سريعة (للتجربة فقط):**

إذا تبي تجرّب بسرعة، استخدم هذا:

```sql
-- حساب تجريبي (غيّره بعدين!)
INSERT INTO admin_users (email, password_hash, name)
VALUES (
  'admin@test.com',
  '$2b$10$XqL9z8jKGYmXj9Fh7F8kPe3vL.sKq.wL3/M8F8k.7M8F8k.7M8F8k.',
  'Admin'
);
```

**معلومات الدخول:**
- Email: `admin@test.com`
- Password: `Test123!`

⚠️ **تحذير أمني:** غيّر هذا الحساب بأسرع وقت! هذا للتجربة فقط.

---

## 🔐 **نصائح أمنية:**

✅ **استخدم باسورد قوي** (12+ حرف، أرقام، رموز)
✅ **غيّر الباسورد بشكل دوري**
✅ **لا تشارك بيانات الأدمن مع أحد**
❌ **لا تستخدم "admin123" أو "password"**

---

## 🎯 **بعد الإنشاء:**

الحين تقدر:
1. ✅ تسجيل الدخول لصفحة `/admin/login`
2. ✅ مراجعة المقارنات المعلّقة
3. ✅ الموافقة/الرفض على المقارنات
4. ✅ مشاهدة الإحصائيات

---

**محتاج مساعدة؟** راجع ملف `SUPABASE_MIGRATION_INSTRUCTIONS.md` للتعليمات الكاملة.
